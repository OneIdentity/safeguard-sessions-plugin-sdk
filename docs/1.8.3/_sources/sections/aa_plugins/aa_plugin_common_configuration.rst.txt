.. _aa-plugin-config-label:

Common configuration options
----------------------------

The common AA plugin functionality can be configured by the following configuration options.

.. literalinclude:: ../../../build/aa_plugin_config.cfg
    :language: ini

*New in version 1.8.0.* AA plugin can be configured to ignore the suffix part of UPN usernames in RDP connections

*New in version 1.4.0.* HTTPS proxy configuration can be overridden from plugin config, and
password can be stored in a credential store.