.. _cred_store_common_config:

Common configuration options
----------------------------

The common credential store plugin functionality can be configured by the following configuration options.

.. literalinclude:: ../../../build/credentialstore_plugin_config.cfg
    :language: ini

*New in version 1.4.0.* HTTPS proxy configuration can be overridden from plugin config, and
password can be stored in a credential store.
